## Your condition: C (progressive capture)
Build in four visual slices and run the Render loop (check, capture, open the PNG, read the snapshot and receipt) at the end of each slice before starting the next:
1. Frame and header row only (capture initial state).
2. Add the seven day cells (capture initial state).
3. Add the progress bar (capture initial state).
4. Add the buttons and storage (capture initial state AND after-clicks state).
Fix what you see at each slice before moving on. You may re-capture within a slice as often as you like. After slice 4, keep iterating until you are satisfied.
